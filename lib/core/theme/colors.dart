import 'package:flutter/material.dart';

// Latar Belakang & Teks Dasar
const Color kBackgroundColor = Color(0xFFFFFFFF); // Latar belakang utama
const Color kTextColor = Color(0xFF37353E); // Teks utama
const Color kSecondaryTextColor = Colors.grey; // Teks sekunder

// Warna Aksen & Komponen Gelap
const Color kPrimaryColor = Color(0xFF715A5A); // Warna Aksen Utama
const Color kDarkBlockColor = Color(
  0xFF37353E,
); // Warna blok (Menu Block, Bottom Nav)
const Color kDarkComponentColor = Color(0xFF44444E); // Warna komponen
const Color kLightTextColor = Color(0xFFFFFFFF); // Teks putih
